# ~/.config/hypr/hyprsunset.conf

max-gamma = 150  # 伽马值上限（绝对最大值200）

profile {
    time = 7:30   # 早晨7:30恢复默认色温
    identity = true  # 重置为默认（色温6500K，伽马1.0）
}

profile {
    time = 21:00  # 晚上9:00启用暖色温和低亮度
    temperature = 5500  # 色温（值越低越暖）
    gamma = 0.8   # 亮度（0.8=80%）
}
